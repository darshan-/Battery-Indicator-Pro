<?xml version="1.0" encoding="utf-8"?>
<!-- I'm not 100% sure it's kosher to use @+android:id/summary, but it allows setting text with setSummary() and works on my Pixel 2. -->
<Button xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/summary"
    android:layout_height="@dimen/tls_button_height"
    android:layout_width="fill_parent"
    android:layout_gravity="center_horizontal"
    android:onClick="enableNotifsButtonClick"
    android:textSize="@dimen/tls_button_fs"
/>
